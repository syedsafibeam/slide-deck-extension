# Product Packs

Put optional Designer or Smart Tables variants here and reference them from `contributes.productPacks`.

Use `targetAgentId: "built-in:design"` with `kind: "designer"` for presentation/deck packs.
Use `targetAgentId: "built-in:smart-tables"` with `kind: "smart_tables"` for evidence matrix/workbook packs.

